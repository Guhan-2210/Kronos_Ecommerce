{
  "name": "fulfilment-worker",
  "version": "1.0.0",
  "description": "Fulfilment and delivery microservice with Cloudflare Workers",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8791",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "NODE_OPTIONS=--experimental-vm-modules mocha 'test/**/*.test.js' --timeout 5000",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules mocha 'test/**/*.test.js' --watch",
    "test:coverage": "NODE_OPTIONS=--experimental-vm-modules c8 mocha 'test/**/*.test.js' --timeout 5000"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "fulfilment",
    "delivery"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@microlabs/otel-cf-workers": "^1.0.0-rc.52",
    "@opentelemetry/api": "^1.9.0",
    "itty-router": "^5.0.0",
    "joi": "^17.11.0",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "c8": "^8.0.1",
    "chai": "^4.3.10",
    "chai-as-promised": "^8.0.2",
    "mocha": "^10.2.0",
    "sinon": "^17.0.1",
    "wrangler": "^3.80.0"
  }
}
